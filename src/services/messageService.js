// ========================================
// メッセージ処理サービス
// ========================================
// ユーザーから届いたメッセージを解析し、
// 適切な返答を組み立てるロジックをここに書きます。
//
// routes/webhook.js からこのサービスを呼び出す予定です。

/**
 * ユーザーのメッセージを受け取り、返答テキストを返す
 * @param {string} userMessage - ユーザーが送ったテキスト
 * @returns {string} ボットの返答テキスト
 */
export function buildReply(userMessage) {
  // TODO: メッセージの内容に応じた返答ロジックを実装する
  // 例: 「授乳」と送られたら授乳記録の案内を返す

  return `「${userMessage}」を受け取りました。（返答は今後実装します）`;
}
